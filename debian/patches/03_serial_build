Description: Do not fail if MIN is already defined
Author: Ludovic Rousseau <rousseau@debian.org>
--- a/asedriveiiieserial/Ase.h
+++ b/asedriveiiieserial/Ase.h
@@ -46,9 +46,10 @@
 #define PRINT_CHAR(c) LEFT_NIBBLE(c), RIGHT_NIBBLE(c)
 
 
+#ifndef MIN
 #define MIN(a, b) (a > b ? b : a)
 #define MAX(a, b) (a > b ? a : b)
-
+#endif
 
 #define ASE_READER_ETU_DELTA       150
 
