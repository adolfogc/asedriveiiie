Description: do not link the drivers with libpcsclite.so 
Author: Ludovic Rousseau <rousseau@debian.org>
--- a/asedriveiiie-serial/configure
+++ b/asedriveiiie-serial/configure
@@ -69,6 +69,6 @@
 echo "USBDROPDIR=${USBDROPDIR}" >> Makefile.inc
 echo "BUILD=${BUILD}" >> Makefile.inc
 echo "CFLAGS=${CFLAGS}" >> Makefile.inc
-echo "LDFLAGS=${LDFLAGS}" >> Makefile.inc
+#echo "LDFLAGS=${LDFLAGS}" >> Makefile.inc
 
 exit 0
--- a/asedriveiiie-usb/configure
+++ b/asedriveiiie-usb/configure
@@ -69,6 +69,6 @@
 echo "USBDROPDIR=${USBDROPDIR}" >> Makefile.inc
 echo "BUILD=${BUILD}" >> Makefile.inc
 echo "CFLAGS=${CFLAGS}" >> Makefile.inc
-echo "LDFLAGS=${LDFLAGS}" >> Makefile.inc
+#echo "LDFLAGS=${LDFLAGS}" >> Makefile.inc
 
 exit 0
